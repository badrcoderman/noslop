# Upstream Checkouts

This directory is intentionally ignored except for this file. The checkouts are
local, pinned reference trees and are not part of the noslop release artifact.

The authoritative pins are in `../manifests/upstream-sources.json`.

Do not edit these checkouts. Add newly authored code under `../src/` and record
any source comparison in `../docs/` or `../findings/`.
