# Findings

This directory stores noslop-specific findings after the evidence is collected.

The existing FW 12.00 research ledger remains external and append-only at:

`/home/user/Documents/webp5/ps5-libs/research_12_00/state/findings.jsonl`

The next existing finding ID is `F-021`.

Do not promote a finding from a Discord report, string, import, or offset file
alone. Each finding needs exact firmware, reachability, attacker control, and a
safe reproducible proof.
